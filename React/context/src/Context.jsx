import { createContext } from "react";
import Context from "./Context";

let Context=createContext()
function reduser(apiD)
let [val,dispatch]=useReducer(reduser,data)
return(<div>
  <Context.Provider>
    {Children}
  </Context.Provider>
</div>)

export default Context